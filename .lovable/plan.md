

The user wants to add password reset functionality from the admin panel. The `reset-password` edge function already exists. I need to check the current Usuarios.tsx page to see what's already there and design the UI addition.

Let me check the existing admin Usuarios page to understand the current structure.
